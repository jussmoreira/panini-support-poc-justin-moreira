package com.panini.support.core

/**
 * App-wide technical constants.
 * When the backend is ready, only BASE_URL needs to change here.
 */
object AppConstants {
    const val BASE_URL = "https://api.panini.support/v1/"
}
